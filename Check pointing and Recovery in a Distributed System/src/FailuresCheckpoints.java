
public class FailuresCheckpoints {
	int seq, nodeid, numCheckpoints;

	public FailuresCheckpoints(int seq, int nodeid, int numCheckpoints) {
		this.seq = seq;
		this.nodeid = nodeid;
		this.numCheckpoints = numCheckpoints;
	}

}
